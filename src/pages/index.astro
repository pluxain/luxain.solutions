---
// Only here to redirect to one of the locale
// @see https://docs.astro.build/en/guides/internationalization/#browser-language-detection

export const prerender = false;
const { preferredLocale } = Astro;
const to = preferredLocale ?? "en";
return Astro.redirect(`/${to}/`);
---
