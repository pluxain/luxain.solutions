{
  "pattern": "^(main){1}$|^(f|b|h|c)/(\\d+).+$",
  "errorMsg": "Branch name must start with `f/`(feature), `b/`(bugfix), `h/`(hotfix), `c/` (chore) and be followed by `xxx` where xxx is the issue number"
}
